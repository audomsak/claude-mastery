---
title: "Day 118: Build Enterprise MCP Server (Part 2)"
description: "Observability, rate limiting, deploy"
---

# Day 118: Enterprise MCP — Part 2 🚀

<div class="lesson-meta">
⏱️ 4 ชั่วโมง &nbsp;|&nbsp; 📊 Project &nbsp;|&nbsp; 📋 Prerequisites: Day 117
</div>

## 🎯 Goal

Production-ready: observability, rate limit, CI/CD, deploy

---

## 1. Observability

```python
# observability/tracing.py
from opentelemetry import trace, metrics
from opentelemetry.exporter.otlp.proto.grpc.trace_exporter import OTLPSpanExporter
from opentelemetry.sdk.trace import TracerProvider
from opentelemetry.sdk.trace.export import BatchSpanProcessor
from opentelemetry.sdk.metrics import MeterProvider
from opentelemetry.sdk.resources import Resource

def setup_telemetry():
    resource = Resource.create({"service.name": "servicedesk-mcp"})
    
    # Traces
    trace_provider = TracerProvider(resource=resource)
    trace_provider.add_span_processor(BatchSpanProcessor(OTLPSpanExporter()))
    trace.set_tracer_provider(trace_provider)
    
    # Metrics
    metric_provider = MeterProvider(resource=resource)
    metrics.set_meter_provider(metric_provider)

tracer = trace.get_tracer(__name__)
meter = metrics.get_meter(__name__)

# Counters / Histograms
TOOL_CALLS = meter.create_counter("mcp_tool_calls", description="Tool calls")
TOOL_LATENCY = meter.create_histogram("mcp_tool_latency_ms", description="Latency")
TOOL_ERRORS = meter.create_counter("mcp_tool_errors", description="Errors")
DB_QUERIES = meter.create_counter("mcp_db_queries", description="DB queries")
```

---

## 2. Decorate Tools

```python
# tools/tickets.py (continued)
import time

@server.call_tool()
@require_auth()
async def call_tool(name, args, user_info):
    with tracer.start_as_current_span(f"tool.{name}") as span:
        span.set_attribute("user.id", user_info.user_id)
        span.set_attribute("tenant.id", user_info.tenant_id)
        span.set_attribute("tool.name", name)
        
        TOOL_CALLS.add(1, {"tool": name, "tenant": user_info.tenant_id})
        
        start = time.time()
        try:
            result = await dispatch(name, args, user_info)
            elapsed_ms = (time.time() - start) * 1000
            TOOL_LATENCY.record(elapsed_ms, {"tool": name})
            return result
        except Exception as e:
            TOOL_ERRORS.add(1, {"tool": name, "error_type": type(e).__name__})
            span.record_exception(e)
            raise
```

---

## 3. Rate Limiting

```python
# auth/rate_limit.py
from collections import defaultdict
from datetime import datetime, timedelta
import asyncio

class TokenBucket:
    def __init__(self, capacity, refill_per_sec):
        self.capacity = capacity
        self.refill_per_sec = refill_per_sec
        self.tokens = capacity
        self.last_refill = datetime.now()
        self.lock = asyncio.Lock()
    
    async def take(self, n=1):
        async with self.lock:
            now = datetime.now()
            elapsed = (now - self.last_refill).total_seconds()
            self.tokens = min(self.capacity, self.tokens + elapsed * self.refill_per_sec)
            self.last_refill = now
            
            if self.tokens >= n:
                self.tokens -= n
                return True
            return False

# Per-user limits per tool
limits = defaultdict(lambda: TokenBucket(60, 60/60))  # 60/min

async def check_rate_limit(user_id, tool_name):
    key = f"{user_id}:{tool_name}"
    bucket = limits[key]
    if not await bucket.take():
        raise RateLimitExceeded(f"Limit exceeded for {tool_name}")
```

For production: use Redis-backed rate limiter for distributed deployment

---

## 4. Health Checks

```python
# server.py
from starlette.routing import Route
from starlette.responses import JSONResponse

async def health(request):
    return JSONResponse({"status": "ok", "version": APP_VERSION})

async def ready(request):
    # Deeper checks
    db_ok = await check_db()
    idp_ok = await check_idp()
    return JSONResponse({
        "status": "ready" if db_ok and idp_ok else "not_ready",
        "db": db_ok,
        "idp": idp_ok
    }, status_code=200 if db_ok and idp_ok else 503)

app.routes.extend([
    Route("/health", health),
    Route("/ready", ready)
])
```

---

## 5. Dockerfile

```dockerfile
FROM python:3.12-slim

WORKDIR /app

# Install deps
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy code
COPY . .

# Non-root user
RUN useradd -m mcpuser
USER mcpuser

# Health check
HEALTHCHECK --interval=30s --timeout=5s --start-period=10s \
  CMD curl -f http://localhost:8000/health || exit 1

EXPOSE 8000
CMD ["uvicorn", "server:app", "--host", "0.0.0.0", "--port", "8000"]
```

---

## 6. CI/CD

```yaml
# .github/workflows/mcp-ci.yml
name: MCP CI/CD
on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: test
        options: --health-cmd pg_isready --health-interval 10s
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.12' }
      - run: pip install -r requirements.txt
      - run: pytest tests/ -v --cov
      - run: ruff check .
      - run: mypy server.py tools/ auth/
  
  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: pip install safety bandit
      - run: safety check
      - run: bandit -r . -ll
  
  build_deploy:
    needs: [test, security]
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: docker build -t $IMAGE:$GITHUB_SHA .
      - name: Push
        run: docker push $IMAGE:$GITHUB_SHA
      - name: Deploy (staging)
        run: ./scripts/deploy.sh staging $GITHUB_SHA
      - name: Smoke test
        run: python scripts/smoke.py https://mcp-staging.example.com
```

---

## 7. Terraform Deploy

```hcl
# infra/main.tf
resource "aws_ecs_service" "mcp" {
  name            = "servicedesk-mcp"
  cluster         = aws_ecs_cluster.main.id
  task_definition = aws_ecs_task_definition.mcp.arn
  desired_count   = 3
  
  load_balancer {
    target_group_arn = aws_lb_target_group.mcp.arn
    container_name   = "mcp"
    container_port   = 8000
  }
  
  network_configuration {
    subnets          = var.private_subnets
    security_groups  = [aws_security_group.mcp.id]
  }
}

resource "aws_lb" "mcp" {
  name               = "mcp-alb"
  load_balancer_type = "application"
  subnets            = var.public_subnets
  
  enable_deletion_protection = true
}

resource "aws_lb_target_group" "mcp" {
  port        = 8000
  protocol    = "HTTP"
  target_type = "ip"
  vpc_id      = var.vpc_id
  
  health_check {
    path                = "/health"
    matcher             = "200"
    interval            = 30
  }
}

resource "aws_acm_certificate" "mcp" {
  domain_name       = "mcp.example.com"
  validation_method = "DNS"
}

resource "aws_lb_listener" "https" {
  load_balancer_arn = aws_lb.mcp.arn
  port              = 443
  protocol          = "HTTPS"
  certificate_arn   = aws_acm_certificate.mcp.arn
  
  default_action {
    type             = "forward"
    target_group_arn = aws_lb_target_group.mcp.arn
  }
}
```

---

## 8. Auto-scaling

```hcl
resource "aws_appautoscaling_target" "mcp" {
  service_namespace  = "ecs"
  resource_id        = "service/${aws_ecs_cluster.main.name}/${aws_ecs_service.mcp.name}"
  scalable_dimension = "ecs:service:DesiredCount"
  min_capacity       = 3
  max_capacity       = 20
}

resource "aws_appautoscaling_policy" "cpu" {
  name               = "cpu-target"
  policy_type        = "TargetTrackingScaling"
  resource_id        = aws_appautoscaling_target.mcp.resource_id
  scalable_dimension = aws_appautoscaling_target.mcp.scalable_dimension
  service_namespace  = aws_appautoscaling_target.mcp.service_namespace
  
  target_tracking_scaling_policy_configuration {
    target_value = 60
    predefined_metric_specification {
      predefined_metric_type = "ECSServiceAverageCPUUtilization"
    }
  }
}
```

---

## 9. Smoke Test

```python
# scripts/smoke.py
import asyncio
from mcp.client.streamable_http import streamablehttp_client
from mcp import ClientSession

async def smoke(url):
    async with streamablehttp_client(url) as (read, write, _):
        async with ClientSession(read, write) as session:
            await session.initialize()
            
            # List tools
            tools = await session.list_tools()
            assert len(tools.tools) >= 4, f"Expected ≥4 tools, got {len(tools.tools)}"
            
            # Call a tool (with test token)
            # ... (auth headers etc.)
            
            print("✅ Smoke tests passed")

if __name__ == "__main__":
    import sys
    asyncio.run(smoke(sys.argv[1] + "/mcp"))
```

---

## 10. Day 118 Deliverables

- [ ] Tracing + metrics integrated
- [ ] Rate limiting per user/tool
- [ ] Health + ready endpoints
- [ ] Dockerfile
- [ ] CI pipeline (test + security + deploy)
- [ ] Terraform deploy to ECS
- [ ] Auto-scaling configured
- [ ] Smoke tests passing
- [ ] Server live at https://mcp-staging.example.com

[ต่อไป → Day 119: MCP Marketplace :material-arrow-right:](day-119.md){ .md-button .md-button--primary }
